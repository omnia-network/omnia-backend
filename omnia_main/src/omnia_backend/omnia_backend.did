type Profile = record {
    "name": text;
    "description": text;
    "keywords": vec text;
};

service : {
    "set_environment_uid": (text) -> (text);
    "whoami": () -> (principal);
    "get_device_uid": () -> (text) query;
    "getSelf": () -> (Profile);

}
